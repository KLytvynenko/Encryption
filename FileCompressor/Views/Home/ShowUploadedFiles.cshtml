@model List<FileInfo>
@{
    ViewBag.Title = "ShowUploadedFiles";
}

@if (Model != null)
{
 foreach (var file in Model)
 {
     <b>@Html.ActionLink(file.ToString(), "ShowImage", new {id = file.Name})</b>
     <br>
 }
}
